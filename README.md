#Jira SLA config

Docker network demo, jira with sonar network ( may drop if suitable sonar endpoint becomes available)

## Requirements

 Docker
 
 Docker Compose


> Clone repo
> 
> $ docker-compose up -d
